# Contoso stub configuration — used by Contoso.ConfigurationAdapter in dev/standalone deployments.
# Format: flat key-value, one entry per line: setting.path = value
# Keys are matched case-insensitively against the settingPath requested by jobs.
#
# WARNING: Do not commit real configuration values. This file is for development use only.
#          In production deployments, replace with a real ConfigurationAdapter EXE backed
#          by the company configuration store and remove this file from the Adapters/ folder.

jobs.demo.max-parallel    = 3
jobs.demo.timeout-minutes = 10
platform.environment      = DEV
